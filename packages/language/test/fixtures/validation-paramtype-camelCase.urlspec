namespace jobs;

param sortOrder = "recent" | "popular";
param jobStatus = "active" | "closed";
param direction = "asc" | "desc";

page list = /jobs {
  sort: sortOrder;
  status?: jobStatus;
  order: direction;
}
