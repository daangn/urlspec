// Job listing and detail pages
namespace jobs;

// Sort order for job listings
param sortOrder = "recent" | "popular" | "trending";

global {
  // Referrer source
  referrer?: string;
}

// Job listings page
page list = /jobs {
  // Job category filter
  category?: string;

  // Sort order
  sort: sortOrder;
}
